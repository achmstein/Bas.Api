{
  "//": [
    "Copy to appsettings.Development.json (git-ignored) for local work.",
    "Partners are registered through the admin console at /admin/partners (or",
    "POST /admin/v1/partners), which issues their API key - nothing partner-related",
    "is configured in files."
  ],
  "ConnectionStrings": {
    "basdb": "Host=localhost;Port=5432;Database=basdb;Username=postgres;Password=postgres"
  },
  "PartnerAuth": {
    "Issuer": "http://localhost:5280"
  },
  "Cors": {
    "AllowedOrigins": [ "http://localhost:3000" ]
  }
}
