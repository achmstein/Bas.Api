# xunit v3 runs on Microsoft.Testing.Platform. The .NET 10 SDK no longer drives it through VSTest,
# so `dotnet test` has to be told which runner to use.
[dotnet.test:runner]
name = "Microsoft.Testing.Platform"
